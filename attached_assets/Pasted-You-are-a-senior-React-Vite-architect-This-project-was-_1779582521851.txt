You are a senior React/Vite architect. This project was generated by Lovable and needs a professional refactor. Do NOT change any functionality, UI, or logic behavior — only reorganize and clean up the code structure.

## Goals

### 1. Folder Structure
Reorganize into a scalable structure:
src/
  components/       # Shared/reusable UI components
    ui/             # Primitive components (buttons, inputs, etc.)
  features/         # Feature-based modules (each with own components, hooks, types)
  hooks/            # Global custom hooks
  pages/            # Route-level page components
  styles/           # Global styles, Tailwind config overrides
  lib/              # Utilities, helpers, constants
  types/            # Global TypeScript types/interfaces

### 2. Break Down Large Components
- Any component over ~150 lines should be split into smaller sub-components
- Extract JSX sections into clearly named child components
- Keep each file focused on a single responsibility

### 3. Deduplicate Logic & Custom Hooks
- Identify repeated logic across components
- Extract into custom hooks under src/hooks/ (or feature-level hooks/)
- Name hooks clearly: useAuth, useFormValidation, useMediaQuery, etc.

### 4. Clean Up Tailwind / Styles
- Replace repeated Tailwind class strings with reusable component variants (using cva or clsx if already in project)
- Remove duplicate or conflicting class names
- Group responsive/mobile classes consistently (mobile-first: sm:, md:, lg:)

## Rules
- Do not change any component's visible output or behavior
- Do not remove or rename any exported functions/components without updating all imports
- Update all import paths after moving files
- Keep TypeScript types strict — do not use `any`
- After each file change, confirm imports are consistent

## Start by
1. Listing all files and their current sizes (lines of code)
2. Proposing the new folder structure before making changes
3. Asking for confirmation before proceeding with the refactor